CREATE TABLE galleryHasTag (
  galleryId INTEGER NOT NULL,
  tagId INTEGER NOT NULL,
  FOREIGN KEY(galleryId) REFERENCES galleryEntity(id) ON DELETE CASCADE,
  FOREIGN KEY(tagId) REFERENCES tagEntity(id) ON DELETE CASCADE
);

insertAssociation:
INSERT INTO galleryHasTag (galleryId, tagId)
VALUES ?;
